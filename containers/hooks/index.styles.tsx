import { styled } from "@mui/material"

export const MainWrapper = styled("main")({
  padding: "3rem 5rem",
  alignItems: "center"
})

export const Title = styled("h1")({
  margin: 0,
  lineHeight: 1.15,
  fontSize: "4rem",
  textAlign: "center"
})
